# Kafka / S3
aiokafka==0.10.0
boto3==1.34.162
botocore==1.34.162
python-dotenv==1.0.1

# 모델 스택 (GPU 이미지에 torch 포함됨 → torch 제거)
numpy==1.24.3
transformers==4.35.2
tokenizers==0.15.0
sentencepiece==0.1.99
tqdm==4.66.1
pathlib2==2.3.7

# (선택) Kafka 압축 코덱 대응
lz4==4.3.3
zstandard==0.22.0
# python-snappy==0.6.1  # 브로커가 snappy면 필요(시스템 libsnappy 필요)
